# Secrets — never commit
.env

# Node
node_modules/
dist/

# Pulumi state (managed in Pulumi Cloud, not git)
.pulumi/

# macOS
.DS_Store

# Editor
.vscode/
.idea/
*.swp

# Real household budgets — real balances, real birthdays. Never commit these.
# The folder itself is tracked (via .keep) so it's there on a fresh clone.
user-budgets/*
!user-budgets/.keep
